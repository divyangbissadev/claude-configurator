{
  "mcpServers": {
    "azure-devops": {
      "command": "npx",
      "args": ["-y", "@anthropic/azure-devops-mcp"],
      "env": {
        "AZURE_DEVOPS_ORG": "${ADO_ORG}",
        "AZURE_DEVOPS_PAT": "${ADO_PAT}"
      }
    }
  }
}
