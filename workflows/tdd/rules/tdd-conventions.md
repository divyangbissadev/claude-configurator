---
paths:
  - "**/*"
---

# TDD Conventions

- Write the test BEFORE the implementation — no exceptions
- Each test verifies one behavior
- Run tests after every change — red or green, never unknown
- Refactor only when tests are green
- Commit after each passing test, not in batches
